{
    "configurations": [
        {
            "name": "Python: File",
            "type": "python",
            "request": "launch",
            "cwd": "${fileDirname}",
            "program": "${file}",
            "justMyCode": true
        }
    ]
}